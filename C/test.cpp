/*
g++ test.cpp
./a.out

Control + option + Nで実行
*/

#include <bits/stdc++.h>
using namespace std;

int main() {
    int N;
    cin >> N;

    vector<vector<int>> vec(N, vecotr<int>(9, 1));

    for (int i = 1; i < N; i++)
    {
        for (int j = 0; j < 9; j++)
        {
            for (int k = 0; k < count; k++)
            {
                /* code */
            }
            
        }
        
    }
    
    return 0;
}
